{
  "name": "postiz-command",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "dotenv -e ../../.env -- nest start --watch --entryFile=./apps/command/src/main",
    "build": "echo 'Building commands app (compiles shared libraries; may take several minutes)...' && cross-env NODE_ENV=production nest build && echo 'Commands build complete.'",
    "start": "dotenv -e ../../.env -- node ./dist/apps/commands/src/main.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
